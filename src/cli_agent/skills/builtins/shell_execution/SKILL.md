# Shell Execution Skill

## Overview
Executes local system shell commands with built-in line-filtering (500 lines cap) and cross-platform security guardrails.

## Parameters
- `command` (string, required): Shell command line string to execute.
