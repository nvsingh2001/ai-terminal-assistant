# File Management Skill

## Overview
Reads, writes, appends, and lists files in local workspace with sensitive credential protection and size limits.

## Parameters
- `action` (string, required): Operation type: 'read', 'write', 'list', 'append'.
- `path` (string, required): Target file or directory path.
- `content` (string, optional): Text content to write or append.
